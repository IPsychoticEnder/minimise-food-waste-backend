This file is a merged representation of the entire codebase, combined into a single document by Repomix.

<file_summary>
This section contains a summary of this file.

<purpose>
This file contains a packed representation of the entire repository's contents.
It is designed to be easily consumable by AI systems for analysis, code review,
or other automated processes.
</purpose>

<file_format>
The content is organized as follows:
1. This summary section
2. Repository information
3. Directory structure
4. Repository files (if enabled)
5. Multiple file entries, each consisting of:
  - File path as an attribute
  - Full contents of the file
</file_format>

<usage_guidelines>
- This file should be treated as read-only. Any changes should be made to the
  original repository files, not this packed version.
- When processing this file, use the file path to distinguish
  between different files in the repository.
- Be aware that this file may contain sensitive information. Handle it with
  the same level of security as you would the original repository.
</usage_guidelines>

<notes>
- Some files may have been excluded based on .gitignore rules and Repomix's configuration
- Binary files are not included in this packed representation. Please refer to the Repository Structure section for a complete list of file paths, including binary files
- Files matching patterns in .gitignore are excluded
- Files matching default ignore patterns are excluded
- Files are sorted by Git change count (files with more changes are at the bottom)
</notes>

</file_summary>

<directory_structure>
.gitignore
eslint.config.js
index.html
package.json
public/vite.svg
README.md
src/App.css
src/App.tsx
src/assets/img/ingredients-bg.png
src/common.css
src/components/Home-Page/generate-recipes-button/GenerateButton.css
src/components/Home-Page/generate-recipes-button/GenerateButton.tsx
src/components/nav-bar/NavBar.css
src/components/nav-bar/NavBar.tsx
src/components/photo-banner/PhotoBanner.css
src/components/photo-banner/PhotoBanner.tsx
src/components/profile-popup/ProfilePopup.tsx
src/components/reusable-blur/ReusableBlur.css
src/components/reusable-blur/ReusableBlur.tsx
src/components/reusable-dropdown/ReusableDropdown.css
src/components/reusable-dropdown/ReusableDropdown.tsx
src/index.css
src/main.tsx
src/pages/home-page/HomePage.tsx
src/pages/sign-up-page/SignUpForm.css
src/pages/sign-up-page/SignUpForm.tsx
tsconfig.app.json
tsconfig.json
tsconfig.node.json
vite.config.ts
</directory_structure>

<files>
This section contains the contents of the repository's files.

<file path=".gitignore">
# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
</file>

<file path="eslint.config.js">
import js from '@eslint/js'
import globals from 'globals'
import reactHooks from 'eslint-plugin-react-hooks'
import reactRefresh from 'eslint-plugin-react-refresh'
import tseslint from 'typescript-eslint'
import { defineConfig, globalIgnores } from 'eslint/config'

export default defineConfig([
  globalIgnores(['dist']),
  {
    files: ['**/*.{ts,tsx}'],
    extends: [
      js.configs.recommended,
      tseslint.configs.recommended,
      reactHooks.configs.flat.recommended,
      reactRefresh.configs.vite,
    ],
    languageOptions: {
      ecmaVersion: 2020,
      globals: globals.browser,
    },
  },
])
</file>

<file path="index.html">
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <link rel="icon" type="image/svg+xml" href="/vite.svg" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>min-food-waste-frontend</title>
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/main.tsx"></script>
  </body>
</html>
</file>

<file path="package.json">
{
  "name": "min-food-waste-frontend",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "lint": "eslint .",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^19.2.0",
    "react-dom": "^19.2.0",
    "react-icons": "^5.5.0",
    "react-router-dom": "^7.13.0"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.1",
    "@types/node": "^24.10.1",
    "@types/react": "^19.2.5",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^5.1.1",
    "eslint": "^9.39.1",
    "eslint-plugin-react-hooks": "^7.0.1",
    "eslint-plugin-react-refresh": "^0.4.24",
    "globals": "^16.5.0",
    "typescript": "~5.9.3",
    "typescript-eslint": "^8.46.4",
    "vite": "^7.2.4"
  }
}
</file>

<file path="public/vite.svg">
<svg xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink" aria-hidden="true" role="img" class="iconify iconify--logos" width="31.88" height="32" preserveAspectRatio="xMidYMid meet" viewBox="0 0 256 257"><defs><linearGradient id="IconifyId1813088fe1fbc01fb466" x1="-.828%" x2="57.636%" y1="7.652%" y2="78.411%"><stop offset="0%" stop-color="#41D1FF"></stop><stop offset="100%" stop-color="#BD34FE"></stop></linearGradient><linearGradient id="IconifyId1813088fe1fbc01fb467" x1="43.376%" x2="50.316%" y1="2.242%" y2="89.03%"><stop offset="0%" stop-color="#FFEA83"></stop><stop offset="8.333%" stop-color="#FFDD35"></stop><stop offset="100%" stop-color="#FFA800"></stop></linearGradient></defs><path fill="url(#IconifyId1813088fe1fbc01fb466)" d="M255.153 37.938L134.897 252.976c-2.483 4.44-8.862 4.466-11.382.048L.875 37.958c-2.746-4.814 1.371-10.646 6.827-9.67l120.385 21.517a6.537 6.537 0 0 0 2.322-.004l117.867-21.483c5.438-.991 9.574 4.796 6.877 9.62Z"></path><path fill="url(#IconifyId1813088fe1fbc01fb467)" d="M185.432.063L96.44 17.501a3.268 3.268 0 0 0-2.634 3.014l-5.474 92.456a3.268 3.268 0 0 0 3.997 3.378l24.777-5.718c2.318-.535 4.413 1.507 3.936 3.838l-7.361 36.047c-.495 2.426 1.782 4.5 4.151 3.78l15.304-4.649c2.372-.72 4.652 1.36 4.15 3.788l-11.698 56.621c-.732 3.542 3.979 5.473 5.943 2.437l1.313-2.028l72.516-144.72c1.215-2.423-.88-5.186-3.54-4.672l-25.505 4.922c-2.396.462-4.435-1.77-3.759-4.114l16.646-57.705c.677-2.35-1.37-4.583-3.769-4.113Z"></path></svg>
</file>

<file path="README.md">
# React + TypeScript + Vite

This template provides a minimal setup to get React working in Vite with HMR and some ESLint rules.

Currently, two official plugins are available:

- [@vitejs/plugin-react](https://github.com/vitejs/vite-plugin-react/blob/main/packages/plugin-react) uses [Babel](https://babeljs.io/) (or [oxc](https://oxc.rs) when used in [rolldown-vite](https://vite.dev/guide/rolldown)) for Fast Refresh
- [@vitejs/plugin-react-swc](https://github.com/vitejs/vite-plugin-react/blob/main/packages/plugin-react-swc) uses [SWC](https://swc.rs/) for Fast Refresh

## React Compiler

The React Compiler is not enabled on this template because of its impact on dev & build performances. To add it, see [this documentation](https://react.dev/learn/react-compiler/installation).

## Expanding the ESLint configuration

If you are developing a production application, we recommend updating the configuration to enable type-aware lint rules:

```js
export default defineConfig([
  globalIgnores(['dist']),
  {
    files: ['**/*.{ts,tsx}'],
    extends: [
      // Other configs...

      // Remove tseslint.configs.recommended and replace with this
      tseslint.configs.recommendedTypeChecked,
      // Alternatively, use this for stricter rules
      tseslint.configs.strictTypeChecked,
      // Optionally, add this for stylistic rules
      tseslint.configs.stylisticTypeChecked,

      // Other configs...
    ],
    languageOptions: {
      parserOptions: {
        project: ['./tsconfig.node.json', './tsconfig.app.json'],
        tsconfigRootDir: import.meta.dirname,
      },
      // other options...
    },
  },
])
```

You can also install [eslint-plugin-react-x](https://github.com/Rel1cx/eslint-react/tree/main/packages/plugins/eslint-plugin-react-x) and [eslint-plugin-react-dom](https://github.com/Rel1cx/eslint-react/tree/main/packages/plugins/eslint-plugin-react-dom) for React-specific lint rules:

```js
// eslint.config.js
import reactX from 'eslint-plugin-react-x'
import reactDom from 'eslint-plugin-react-dom'

export default defineConfig([
  globalIgnores(['dist']),
  {
    files: ['**/*.{ts,tsx}'],
    extends: [
      // Other configs...
      // Enable lint rules for React
      reactX.configs['recommended-typescript'],
      // Enable lint rules for React DOM
      reactDom.configs.recommended,
    ],
    languageOptions: {
      parserOptions: {
        project: ['./tsconfig.node.json', './tsconfig.app.json'],
        tsconfigRootDir: import.meta.dirname,
      },
      // other options...
    },
  },
])
```
</file>

<file path="src/App.css">
#root {
  max-width: auto;
  margin: 0;
  padding: 0;
  text-align: center;
}
</file>

<file path="src/App.tsx">
import { useState } from 'react'
import Header from './components/nav-bar/NavBar'
import HomePage from './pages/home-page/HomePage'

import './App.css'
import './common.css'

function App() {
  return (
    <>
      <Header/>
      <HomePage/>
    </>
  )
}

export default App
</file>

<file path="src/common.css">
.link {
    text-decoration: none;
    color: black;
    cursor: pointer;
}

.link:hover {
    color: rgb(127, 127, 127);
}

.button {
    padding: 8px 12px;
    border: 1px solid rgb(127, 127, 127);
    border-radius: 7px;

    font-size: 18px;

    background-color: rgb(247, 247, 247);
}

.button:hover {
    background: rgb(43, 142, 255);
    color: white;
    cursor: pointer;
}

.emphasis-text {
    color: rgb(43, 142, 255);
    font-size: 1.1rem;
}
</file>

<file path="src/components/Home-Page/generate-recipes-button/GenerateButton.css">
.generate-recipes-button {
    margin-top: 40px;
    padding: 30px 60px;
}

.sign-up-link {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 8px;
    width: 100%;
}
</file>

<file path="src/components/Home-Page/generate-recipes-button/GenerateButton.tsx">
import React from 'react'

import ReusableBlur from '../../reusable-blur/ReusableBlur'
import SignUpForm from '../../../pages/sign-up-page/SignUpForm'

import './GenerateButton.css'

type Props = {}

function GenerateButton({}: Props) {
  return (
    <>
      <button className='button generate-recipes-button'>
          Generate Recipes
      </button>
      <div className='sign-up-link'>
        <p>Or</p>
        <p className='emphasis-text link'>sign up</p>
        <p>now!</p>
      </div>
    </>
  )
}

export default GenerateButton
</file>

<file path="src/components/nav-bar/NavBar.css">
.header {
    position: fixed;
    inset: 12px 12px auto 12px;
    z-index: 999;
}

.header-container {
    background-color: rgb(235, 235, 235);
    border: 2px solid black;
    border-radius: 8px;
    min-height: 72px;
    
    display: flex;
    align-items: center;

    padding: 10px 14px;
    box-sizing: border-box;
}

.header-logo {
    height: 100%;
    display: flex;
    align-items: center;
    padding: 0px 10px;
    border-right: 2px solid black;
}

.header-logo-text {
    font-weight: 600;
    font-size: 20px;

    color: black;
    text-decoration: none;
    cursor: default;
}

.header-nav-icon {
    display: flex;
    justify-content: space-between;
    align-items: center;
    flex: 1;
    min-width: 0;
}

.header-nav {
    display: flex;
    justify-content: left;
    gap: clamp(12px, 3vw, 48px);
    padding: 0px 10px;
}

.user-icon {

}
</file>

<file path="src/components/nav-bar/NavBar.tsx">
import React from 'react'

import './NavBar.css'

import { NavLink } from 'react-router-dom'
import { MdAccountCircle } from "react-icons/md";
import ReusableDropdown from '../reusable-dropdown/ReusableDropdown';
import ProfilePopup from '../profile-popup/ProfilePopup';

type Props = {}

function header({}: Props) {
  return (
    <div className='header'>
      <div className='header-container'>
        <div className='header-logo'>
          <p><NavLink className='header-logo-text' to='/'>Minimise Food Waste</NavLink></p>
        </div>

        <div className='header-nav-icon'>
          <div className='header-nav'>
            <p><NavLink className='link' to='/'>Home</NavLink></p>
            <p><NavLink className='link' to='/'>Browse recipe's</NavLink></p>
            <p><NavLink className='link' to='/'>My recipe's</NavLink></p>
            <p><NavLink className='link' to='/'>This week</NavLink></p>
            <p><NavLink className='link' to='/'>Settings</NavLink></p>
          </div>

          <div className='user-icon'>
            <ReusableDropdown
              trigger={
                <div className="user-icon-trigger">
                  <MdAccountCircle size={32} />
                </div>
              }
            >
              <ProfilePopup />
            </ReusableDropdown>
          </div>
        </div>
      </div>
    </div>
  )
}

export default header
</file>

<file path="src/components/photo-banner/PhotoBanner.css">
.Home-Banner {
    min-height: 500px;
    
    background-image: linear-gradient(rgba(0, 0, 0, 0.2), rgba(0, 0, 0, 0.4)), url('../../assets/img/ingredients-bg.png');
    background-size: cover;
    background-position: center center;
    background-repeat: no-repeat;
}
</file>

<file path="src/components/photo-banner/PhotoBanner.tsx">
import React from 'react'

import './PhotoBanner.css'

type Props = {}

function PhotoBanner({}: Props) {
  return (
    <div className='Home-Banner'>
    </div>
  )
}

export default PhotoBanner
</file>

<file path="src/components/profile-popup/ProfilePopup.tsx">
import React from 'react'

type Props = {}

function ProfilePopup({}: Props) {
  return (
    <div>profile-popup</div>
  )
}

export default ProfilePopup
</file>

<file path="src/components/reusable-blur/ReusableBlur.css">
/* Trigger can be styled as needed, this is just a placeholder */
.reusable-blur-trigger {
    display: inline-block; /* Or block, depending on layout */
    cursor: pointer;
  }
  
  /* The main overlay */
  .reusable-blur-overlay {
    position: fixed; /* Cover the entire viewport */
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background-color: rgba(0, 0, 0, 0.6); /* Dimmed background (adjust opacity) */
    /* backdrop-filter: blur(3px); */ /* Optional: Add actual blur (check browser support) */
    display: flex; /* Use flexbox to center content */
    align-items: center;
    justify-content: center;
    z-index: 1000; /* Ensure it's on top of other content */
    opacity: 0; /* Start hidden for transition */
    animation: fadeIn 0.3s forwards; /* Fade-in animation */
  }
  
  /* The content container inside the overlay */
  .reusable-blur-content {
    background-color: white;
    border-radius: 8px;
    box-shadow: 0 4px 15px rgba(0, 0, 0, 0.2);
    max-width: 90%; /* Prevent content from being too wide on large screens */
    max-height: 90%; /* Prevent content from being too tall */
    overflow-y: auto; /* Allow scrolling if content is too tall */
    z-index: 1001; /* Above the overlay background */
    /* Add transform for potential scale animation */
    transform: scale(0.95);
    opacity: 0;
    animation: scaleUp 0.3s 0.1s forwards; /* Scale-up animation, slightly delayed */
  }
  
  /* Optional: Close button style if you add one inside the content */
  .reusable-blur-content .close-button {
    position: absolute;
    top: 10px;
    right: 10px;
    background: none;
    border: none;
    font-size: 1.5rem;
    cursor: pointer;
    color: #888;
  }
  .reusable-blur-content .close-button:hover {
      color: #333;
  }
  
  
  /* Simple fade-in animation for overlay */
  @keyframes fadeIn {
    to {
      opacity: 1;
    }
  }
  
  /* Simple scale/fade animation for content */
  @keyframes scaleUp {
    to {
      opacity: 1;
      transform: scale(1);
    }
  }
</file>

<file path="src/components/reusable-blur/ReusableBlur.tsx">
import { type ReactNode, useEffect, useRef, useState } from "react";

import './ReusableBlur.css'

type Props = {
    trigger: ReactNode;
    children: ReactNode;
}

function ReusableBlur(props: Props) {
    const {
        trigger,
        children
    } = props;

    const [isOpen, setIsOpen] = useState(false);
    const blurRef = useRef<HTMLHtmlElement>(null);

    function openBlur(){
        setIsOpen(true);
    }

    function closeBlur(){
        setIsOpen(false)
    }

    function handleOverlayClick(event: React.MouseEvent<HTMLDivElement>) {
        if (event.target === event.currentTarget) {
            closeBlur();
        }
    }

    useEffect(() => {
        function handleKeyDown(event: KeyboardEvent){
            if (event.key === 'Escape'){
                closeBlur();
            }
        }

        if (isOpen){
            document.addEventListener('keydown', handleKeyDown);
            document.body.style.overflow = 'hidden'
        }else {
            document.removeEventListener('keydown', handleKeyDown);
            document.body.style.overflow = '';
        }

        return () => {
            document.removeEventListener('keydown', handleKeyDown);
            document.body.style.overflow = '';
        }
    }, [isOpen, onclose]);

  return (
    <>
    {/* The Trigger Element */}
    <div className="reusable-blur-trigger" onClick={openBlur}>
        {trigger}
    </div>

    {/* The Overlay and Content (only rendered when isOpen is true) */}
    {isOpen && (
        // Use React Portal here for better stacking context management if needed,
        // but for simplicity, we'll render directly first.
        <div
            className="reusable-blur-overlay"
            onClick={handleOverlayClick} // Close on overlay click
            role="dialog" // More appropriate role for a modal popup
            aria-modal="true" // Indicates content outside is inert
        >
            <div
                className="reusable-blur-content"
                // Optional: Add aria-labelledby/aria-describedby pointing to title/description inside children
            >
                {/* You might want a close button inside the content too */}
                {/* <button onClick={closeBlur} className="close-button">X</button> */}
                {children}
            </div>
        </div>
    )}
</>
  )
}

export default ReusableBlur
</file>

<file path="src/components/reusable-dropdown/ReusableDropdown.css">
.reusable-dropdown {
    position: relative;
    display: inline-block;
}

.reusable-dropdown-trigger {
    cursor: pointer;
    display: inline-block; 
    border: none;
    background: none;
    padding: 0;
    margin: 0;
    outline: none;
  }

.dropdown-content {
    position: absolute;
    right: 0.5rem;
    z-index: 999;
  
    background-color: white;
    border: 1px solid #ccc;
    border-radius: 5px;
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
    min-width: 120px;
    padding: 8px 0; 
    overflow: hidden;
}
</file>

<file path="src/components/reusable-dropdown/ReusableDropdown.tsx">
import React, { type ReactNode, useEffect, useRef, useState } from 'react'

import './ReusableDropdown.css'

type Props = {
    trigger: ReactNode;
    children: ReactNode;

}

function ReusableDropdown(props: Props) {
    const {
        trigger,
        children
    } = props;

    const [isOpen, setIsOpen] = useState(false);
    const dropdownRef = useRef<HTMLDivElement>(null);

    function toggleDropdown(){
        setIsOpen(!isOpen);
    }

    useEffect(() => {
        function handleClickOutside(event: MouseEvent) {
            if (dropdownRef.current && !dropdownRef.current.contains(event.target as Node)){
                setIsOpen(false);
            }
        }
        if (isOpen){
            document.addEventListener('mousedown', handleClickOutside);
        }else {
            document.removeEventListener('mousedown', handleClickOutside);
        }
        console.log('Dropdown isOpen:', isOpen);
        return () => {
            document.removeEventListener('mousedown', handleClickOutside);
        }
    }, [isOpen]);

  return (
    <div className='reusable-dropdown' ref={dropdownRef}>
        <div 
            className='reusable-dropdown-trigger'
            onClick={toggleDropdown}
            role='button'
            tabIndex={0}
            aria-haspopup='true'
            aria-expanded={isOpen}
        >
            {trigger}
        </div>
        {isOpen && (
            <div className='dropdown-content'>
                {children}
            </div>
        )}
    </div>
  )
}

export default ReusableDropdown
</file>

<file path="src/index.css">
:root {
  font-family: system-ui, Avenir, Helvetica, Arial, sans-serif;
  line-height: 1.5;
  font-weight: 400;
}

body {
  margin: 0;
  padding: 0;
}

h1 {

}

p {
  margin: 0px;
  padding: 0px;
}
</file>

<file path="src/main.tsx">
import { StrictMode } from 'react'
import { createRoot } from 'react-dom/client'
import './index.css'
import App from './App.tsx'
import { BrowserRouter } from 'react-router-dom'

createRoot(document.getElementById('root')!).render(
  <StrictMode>
    <BrowserRouter>
      <App />
    </BrowserRouter>
  </StrictMode>,
)
</file>

<file path="src/pages/home-page/HomePage.tsx">
import React from 'react'

import PhotoBanner from '../../components/photo-banner/PhotoBanner'
import GenerateButton from '../../components/Home-Page/generate-recipes-button/GenerateButton'

type Props = {}

function HomePage({}: Props): React.ReactNode {
  return (
    <>
      <PhotoBanner/>
      <GenerateButton/>
    </>
  )
}

export default HomePage
</file>

<file path="src/pages/sign-up-page/SignUpForm.css">
.signup-form {
    min-width: 350px;
    padding: 30px 20px;
}

.signup-form-section {
    display: flex;
    flex-direction: column;
    margin-bottom: 15px;
}

.input-field {
    padding: 8px 10px;
    border: 1px solid rgb(192, 192, 192);
    border-radius: 4px;
}

.signup-button {
    width: 100%;
    padding: 8px;
    background-color: rgb(255, 255, 255);
    border: 1px solid rgb(192, 192, 192);
    border-radius: 4px;
}
</file>

<file path="src/pages/sign-up-page/SignUpForm.tsx">
import React from 'react'

import './SignUpForm.css'

type Props = {}

function SignUpForm({}: Props) {
  return (
    <form className='signup-form'>
        <div className='signup-form-section'>
            <label htmlFor='name'>Name</label>
            <input className='input-field'/>
        </div>

        <div className='signup-form-section'>
            <label htmlFor='email'>Email</label>
            <input className='input-field'/>
        </div>

        <div className='signup-form-section'>
            <label htmlFor='password'>Password</label>
            <input className='input-field'/>
        </div>

        <div className='signup-form-section'>
            <label htmlFor='repeatPassword'>Repeat Password</label>
            <input className='input-field'/>
        </div>

        <button className='button signup-button'>
            <p>Signup</p>
        </button>

        <p>Or login.</p>
    </form>
  )
}

export default SignUpForm
</file>

<file path="tsconfig.app.json">
{
  "compilerOptions": {
    "tsBuildInfoFile": "./node_modules/.tmp/tsconfig.app.tsbuildinfo",
    "target": "ES2022",
    "useDefineForClassFields": true,
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "types": ["vite/client"],
    "skipLibCheck": true,

    /* Bundler mode */
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "verbatimModuleSyntax": true,
    "moduleDetection": "force",
    "noEmit": true,
    "jsx": "react-jsx",

    /* Linting */
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "erasableSyntaxOnly": true,
    "noFallthroughCasesInSwitch": true,
    "noUncheckedSideEffectImports": true
  },
  "include": ["src"]
}
</file>

<file path="tsconfig.json">
{
  "files": [],
  "references": [
    { "path": "./tsconfig.app.json" },
    { "path": "./tsconfig.node.json" }
  ]
}
</file>

<file path="tsconfig.node.json">
{
  "compilerOptions": {
    "tsBuildInfoFile": "./node_modules/.tmp/tsconfig.node.tsbuildinfo",
    "target": "ES2023",
    "lib": ["ES2023"],
    "module": "ESNext",
    "types": ["node"],
    "skipLibCheck": true,

    /* Bundler mode */
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "verbatimModuleSyntax": true,
    "moduleDetection": "force",
    "noEmit": true,

    /* Linting */
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "erasableSyntaxOnly": true,
    "noFallthroughCasesInSwitch": true,
    "noUncheckedSideEffectImports": true
  },
  "include": ["vite.config.ts"]
}
</file>

<file path="vite.config.ts">
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

// https://vite.dev/config/
export default defineConfig({
  plugins: [react()],
})
</file>

</files>
